1. Sign up over at openweathermap.org to get an API key.
2. Fork the project and clone it locally.
3. Create a file at the root of the project called .env.local with the following contents:

REACT_APP_API_KEY = The API key you obtained from openweathermap.org

